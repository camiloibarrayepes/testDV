<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Registers</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-beta.2/css/bootstrap.min.css" integrity="sha384-PsH8R72JQ3SOdhVi3uxftmaW6Vc51MKb0q5P2rRUpPvrszuE4W1povHYgTpBfshb" crossorigin="anonymous">
  </head>
  <body>
    <nav class="navbar navbar-dark bg-dark">
      <a href="" class="navbar-brand mx-auto">Registers</a>
    </nav>

    <div class="row">
      <div class="col-md-7 mt-4 text-center" style="margin-left: 30px">
        <% if(news.length==0) { %>
          <label>There is not any data</label>
        <% }else{ %>
        <table class="table table-striped table-bordered">
          <tr>
            <th>ID</th>
            <th>Nombres</th>
            <th>Apellidos</th>
            <th>Teléfonos</th>
            <th>Direcciones</th>
          </tr>
          <% for (var i = 0; i < news.length; i ++) { %>
            <tr>
              <td><%= news[i].id %></td>
              <td><%= news[i].nombres %></td>
              <td><%= news[i].apellidos %></td>
              <td><%= news[i].telefonos %></td>
              <td><%= news[i].direcciones %></td>
            </tr>
          <% } %>
        </table>
        <% } %>
      </div>
      <div class="col-md-4 mt-4 card">
          <h3 class="card-title">Info Company</h3>
          <div class="form-group">
            <label>Name: <%= companies_info[0].name %></label>
          </div>
          <div class="form-group">
            <label>Register count: </label>
          </div>
          <div class="form-group">
            <label>Last update: <%= last_update %></label>
          </div>
          <a href="/">Add new File</a>
      </div>
    </div>
  </body>
  <script>
  </script>
</html>